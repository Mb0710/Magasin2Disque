spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
  mail:
    host: localhost
    port: 1025
    username: test
    password: test
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

eureka:
  client:
    enabled: false

jwt:
  secret: testSecretKeyForJWTTokenGenerationInTestEnvironmentOnly12345
  expiration: 3600000

app:
  frontend:
    url: http://localhost:3000
  upload:
    dir: ./test-uploads
